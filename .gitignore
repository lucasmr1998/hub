# ── Python ───────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Backups locais de dados prod (LGPD — NUNCA commitar)
_backup_*.json
_backup_*.sql
*.so
*.egg
*.egg-info/
dist/
build/
*.whl

# ── Ambientes virtuais ────────────────────────────────────
venv/
env/
.venv/
n_env/
myenv/

# ── Variáveis de ambiente / segredos ─────────────────────
.env
*.env
.env.*
!.env.example
.env.prod_readonly

# ── MCP / Claude Code internal ───────────────────────────
.mcp.json
.claude/scheduled_tasks.lock

# ── Postman / API specs locais ───────────────────────────
*.postman_collection.json

# ── Media uploads (devem viver em volume bind, nao em git) ─
# Ancorado: ignora so o dump de media no root (MEDIA_ROOT do settings_local) e o
# do projeto, sem pegar a doc (robo/docs/PRODUTO/modulos/workspace/) nem templates.
/workspace/
robo/dashboard_comercial/gerenciador_vendas/workspace/
robo/dashboard_comercial/gerenciador_vendas/media/

# ── Backups pontuais de dados de producao ────────────────
.backup_*.json
.backup_*.sql

# ── Django ───────────────────────────────────────────────
*.sqlite3
*.sqlite3-journal
*.sqlite3-wal
*.sqlite3-shm
db_local.sqlite3
staticfiles/
media/
private_media/

# ── Logs ─────────────────────────────────────────────────
*.log
logs/

# ── IDEs e editores ──────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Cobertura e testes ───────────────────────────────────
.coverage
htmlcov/
.pytest_cache/

# ── Node (se houver) ────────────────────────────────────
node_modules/

# ── Projetos de código (repos git separados) ────────────
megaroleta/
prod/

# ── Dumps e configs locais ────────────────────────────────
*.sql
.claude/settings.local.json
dump_*.sql
web_driver_conversao_lead/dom_capturado/

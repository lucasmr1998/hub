Django==5.2.4
djangorestframework==3.17.1
drf-spectacular==0.29.0
gunicorn==21.2.0
psycopg2-binary==2.9.9
whitenoise==6.6.0
python-decouple==3.8
requests==2.31.0
selenium==4.36.0
Pillow==10.1.0
django-ratelimit==4.1.0
cryptography==44.0.0
ruff==0.9.10
django-csp==4.0
markdown==3.7
bleach==6.1.0
resend==2.29.0
channels==4.1.0
channels-redis==4.2.0
daphne==4.1.0
pgvector==0.3.6
openai==1.54.4

# QR code do link publico de cadastro do People (pure python, gera SVG)
# Extracao de texto de curriculo pra triagem por IA. As duas sao Python puro,
# sem dependencia de sistema, mesmo criterio que escolheu o segno. `.doc`
# (Word binario pre 2007) exigiria LibreOffice no container e por isso saiu
# tambem da lista de formatos aceitos no upload.
pypdf==6.14.2
python-docx==1.2.0
segno==1.6.6

# Leitura do export .xlsx do CRM do HubSoft na pagina de inconsistencias
# (aba Oportunidades). Python puro, sem dependencia de sistema, mesmo criterio
# do pypdf/segno acima.
openpyxl==3.1.5

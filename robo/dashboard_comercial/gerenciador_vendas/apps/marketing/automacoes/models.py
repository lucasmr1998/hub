"""Motor de automação de marketing — APOSENTADO (29/06/2026).

Models removidos (migration DeleteModel) e o código deletado (engine, views, signals,
admin, editor Drawflow, cron, commands). Este app permanece como **husk vazio** só pra
coerência do grafo de migrations (`emails/0001` depende de `automacoes/0004`). A engine
viva é `apps/automacao`. Ver `docs/PRODUTO/modulos/automacao/execution-log.md`.
"""
